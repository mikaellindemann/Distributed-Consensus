<Window x:Class="Client.Views.HistorySelectView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:Client.ViewModels"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:ei="http://schemas.microsoft.com/expression/2010/interactions"
        mc:Ignorable="d"
        Title="HistorySelectView" Height="280" Width="500"
        d:DataContext="{d:DesignInstance viewModels:HistorySelectViewModel}" WindowStartupLocation="CenterScreen">
    <StackPanel Orientation="Vertical">
        <TextBlock Text="History Creation" HorizontalAlignment="Center" FontSize="24" Style="{StaticResource HeaderTextBlockStyle}"></TextBlock>
        <Button Content="Produce" Margin="5" Style="{StaticResource ButtonDefaultStyle}">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="Click">
                    <ei:CallMethodAction TargetObject="{Binding}" MethodName="Produce"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </Button>
        <Button Content="Produce + Collapse" Margin="5" Style="{StaticResource ButtonDefaultStyle}">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="Click">
                    <ei:CallMethodAction TargetObject="{Binding}" MethodName="Collapse"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </Button>
        <Button Content="Produce + Collapse + Reduce" Margin="5" Style="{StaticResource ButtonDefaultStyle}">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="Click">
                    <ei:CallMethodAction TargetObject="{Binding}" MethodName="Create"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </Button>
        <Label Content="{Binding EventViewModel.Name}" HorizontalAlignment="Center" Style="{StaticResource TextLabelStyle}"></Label>
    </StackPanel>
</Window>
